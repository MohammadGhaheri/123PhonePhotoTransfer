# راهنمای سریع فارسی — PhonePhotoTransfer v0.2.2

**سازنده: Mohammad Ghaheri (محمد قاهری)**

PhonePhotoTransfer برای انتقال تعداد زیاد عکس و ویدئو از گوشی Android به Windows با ADB ساخته شده و برای اجرای معمولی به اینترنت یا نصب پکیج Python ثالث نیاز ندارد.

## اجرای سریع

1. پروژه را Clone یا ZIP آن را Extract کنید.
2. گوشی را با کابل USB وصل و USB debugging را فعال کنید.
3. اگر Android پیام RSA نشان داد، `Allow` را بزنید.
4. `doctor.bat` را اجرا کنید.
5. اگر دستگاه با وضعیت `device` دیده شد، `run.bat` را اجرا کنید.
6. پوشه مبدأ را وارد کنید؛ برای دوربین معمولاً `/sdcard/DCIM/Camera` است.
7. پوشه مقصد روی Windows را انتخاب کنید.
8. برای اولین اجرا حالت **Copy** را انتخاب کنید.
9. «شروع انتقال» را بزنید.

## اگر ADB پیدا نشد

برنامه ADB را در PATH، متغیرهای Android SDK، مسیرهای رایج Android Studio و چند مسیر توسعه رایج جست‌وجو می‌کند. همچنین می‌توانید `adb.exe` را دستی انتخاب کنید یا پوشه `platform-tools` را کنار برنامه قرار دهید.

## اگر کابل قطع شد

اجرای انتقال متوقف می‌شود. گوشی را دوباره وصل کنید و برنامه را با همان Source و Destination اجرا کنید. فایل‌های کامل قبلی با کمک SQLite Resume Ledger تشخیص داده و Skip می‌شوند.

## Copy یا Move؟

**Copy:** فایل روی گوشی باقی می‌ماند و گزینه پیشنهادی برای اولین اجرا است.

**Move:** فایل ابتدا دانلود و از نظر اندازه Verify می‌شود و فقط بعد از موفقیت از گوشی حذف می‌شود.

برای فایل‌های مهم، ابتدا Copy انجام دهید و نتیجه را بررسی کنید.

## ساخت EXE

برای استفاده روزمره از `run.bat` استفاده کنید. اگر می‌خواهید EXE بسازید، `build_exe.bat` را اجرا کنید. PyInstaller فقط در مرحله Build لازم است.

## تست پروژه

```powershell
python -m unittest discover -s tests -v
```

راهنمای کامل‌تر در [README.md](README.md) قرار دارد.
